using Microsoft.Xna.Framework.Graphics;

namespace MonogameTetrisClient;

public record Assets(
    Texture2D MinoTexture,
    Texture2D TetrionTexture,
    Texture2D WhiteTexture,
    SpriteFont Font
);
